# Core analytical pipeline (01-05)
pandas>=1.5
numpy>=1.23
scipy>=1.9
scikit-learn>=1.2
openpyxl>=3.1

# Figures (04)
matplotlib>=3.6
seaborn>=0.12
plotly>=5.11        # interactive Sankey exported as standalone HTML (Figure 2d)
pypdf>=4.0          # A4 layout / page transforms for the figure PDFs
reportlab>=4.0      # A4 canvas and author caption
pymupdf>=1.24       # crop and A4 composition of the Figure 3 tree PDF

# Standard library used: io, os, re, sys, time, shutil, sqlite3, pathlib
